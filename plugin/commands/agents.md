---
description: List all registered agents
---

# List Agents

Display all agents currently registered with the crew MCP server.

**Usage**: `/agents`

---

Use the `list-agents` MCP tool (no arguments).
Display results showing agent names, session IDs, folders, and message counts.
